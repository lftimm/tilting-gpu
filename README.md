# Tilting GPU

Self-teaching myself general purpose GPU-compute.
I am using rocm because that's what i have, so be sure to have hipcc installed.

Each folder has its own build system.

```sh
cd example/build && cmake .. && make

```
