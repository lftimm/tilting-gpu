# QueryDevice

Shows how to query device properties in a program.
Basically when running a GPGPU program, we should know its specs so we can better optimize the code.
Stuff like knowing the amount of threads per SM, the amount of registers it might have, grid and block sizes.
